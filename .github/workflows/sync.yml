name: Sync to Client Repositories

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Sync with Client  Repository
        if: success()
        env:
          CLIENT_PATH: ${{ secrets.CLIENT_PATH }}
        run: |
          git remote add client1 https://sharplemon23:${CLIENT_PATH}@github.com/sochperformance/test-web.git
          git push client1 main --force

     